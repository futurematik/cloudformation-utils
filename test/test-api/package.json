{
  "name": "@cfnutil-test/test-api",
  "version": "0.3.0",
  "private": true,
  "dependencies": {
    "cat-me": "^1.0.3",
    "debug": "^4.1.1"
  },
  "devDependencies": {
    "@cfnutil/assets": "^0.4.0"
  },
  "scripts": {
    "build": "yarn compile",
    "bundle": "cfnutil asset rollup --package-install-image node:alpine",
    "compile": "tsc",
    "lint": "eslint src/ --ext=ts,tsx"
  },
  "bundle": {
    "path": "dist/bundle.zip"
  }
}
